# Define the number printed
fibonacci_num_printed = 50

# Function to calculate the next fibonacci number
def cal_fibonacci_num(n):
    if n == 1 or n == 2:
        return 1
    else:
        return cal_fibonacci_num(n-2) + cal_fibonacci_num(n-1)

# Print fibonacci number
for number in range(1, fibonacci_num_printed + 1):
    print(cal_fibonacci_num(number))